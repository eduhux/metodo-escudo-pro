/**
 * Galeria "Escudos desenvolvidos pelo professor" (landing page).
 *
 * Os arquivos ficam em `public/escudos/`. Esta lista é preenchida
 * automaticamente ao otimizar as imagens (resize + compressão).
 * Enquanto estiver vazia, a seção não aparece no site.
 */
export const escudos: string[] = [];

/** Total real de escudos já desenvolvidos (usado no texto de apoio). */
export const totalEscudos = 100;
